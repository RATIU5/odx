---
id: "R3",
class: "dependencies_hidden_state",
statement: "Pure and service packages have no mutable package-scope value declarations, including when branches and foreign blocks; pass state as a parameter.",
why: "This project convention makes mutable state explicit in procedure inputs. The syntax check includes thread-local declarations and inactive branches, but does not establish purity, determinism, or absence of imported state and side effects.",
severity: "error",
instead_of: "Package-level state read and written from several procedures.",
fix_hint: "Move mutable state into a caller-owned value or struct and pass it to the procedures that use it.",
evidence: "Native AST Value_Decl.is_mutable distinguishes variables from constants; package-scope traversal includes when and foreign containers and excludes procedure bodies. Grouped variables produce one finding per declaration.",
cost: "State becomes a parameter or a struct the caller owns; only configured pure and service roles are selected by this rule.",
role: "pure",
check: { kind: "pattern", match: "decl", at: "package_scope", mutable: true, roles: ["pure", "service"] },
---

```odin fires
#+vet explicit-allocators
count: int

bump :: proc() -> int {
	count += 1
	return count
}
```

```odin silent
#+vet explicit-allocators
Counter :: struct {
	count: int,
}

bump :: proc(c: ^Counter) -> int {
	c.count += 1
	return c.count
}
```
