---
id: "R1",
statement: "Use do for an if body containing one return, call, or assignment.",
why: "Keep simple conditionals easy to scan and generate consistently.",
instead_of: "Braces around a single return, call, or assignment.",
fix_hint: "Replace if CONDITION { STATEMENT } with if CONDITION do STATEMENT; preserve comments.",
evidence: "The native Odin AST identifies a braced body and its single statement.",
cost: "Declarations, multiple statements, defer, nested control flow, and if/else chains are excluded; calls are counted syntactically, not by runtime effects.",
severity: "warning",
check: { kind: "pattern", match: "if" },
---

The example intentionally contains three warnings. Use `--strict` to make
warnings fail a check, or change this rule's severity to `error`.
