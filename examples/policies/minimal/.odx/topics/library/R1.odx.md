---
id: "R1",
statement: "Declare no mutable package-scope values, including inactive when branches and foreign blocks.",
why: "Callers own parsing state and can use independent instances of this library.",
instead_of: "A package variable shared by otherwise independent callers.",
fix_hint: "Move the variable into a caller-owned struct or procedure-local value.",
evidence: "Native AST mutable value declarations at package scope; one finding per declaration, including grouped variables.",
cost: "State must be passed explicitly; imported state and runtime side effects are outside this rule.",
severity: "error",
check: { kind: "pattern", match: "decl", at: "package_scope", mutable: true },
---

Constants, struct fields and procedure-local variables are permitted. The rule
examines syntax in every collected source branch; it does not prove purity.

```odin fires
count: int
```

```odin silent
Status :: enum { Ready, End }
Cursor :: struct { offset: int }

Read :: proc() -> Status {
	cursor := Cursor{}
	cursor.offset += 1
	return .End if cursor.offset > 0 else .Ready
}
```
