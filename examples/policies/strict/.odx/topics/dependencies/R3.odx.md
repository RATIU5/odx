---
id: "R3",
statement: "Domain source has no mutable package-scope value declarations, including conditional branches and foreign blocks.",
why: "Callers should supply mutable domain state explicitly.",
instead_of: "Sharing mutable package state between domain operations.",
fix_hint: "Move the state into a caller-owned struct and pass it as a parameter.",
evidence: "Native AST declaration mutability and package-scope traversal; locals and fields are excluded.",
cost: "Includes inactive parsed branches; constants can still refer to mutable data and this does not prove purity.",
severity: "error",
role: "domain",
check: { kind: "pattern", match: "decl", at: "package_scope", mutable: true, roles: ["domain"] },
---

```odin fires
#+vet explicit-allocators
count: int
```

```odin silent
#+vet explicit-allocators
Counter :: struct { count: int }
bump :: proc(c: ^Counter) { c.count += 1 }
```
