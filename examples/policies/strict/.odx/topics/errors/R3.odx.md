---
id: "R3",
statement: "Exported non-test domain procedures whose canonical named final result ends in Domain_Failure or Storage_Failure carry @(require_results).",
why: "Selected domain APIs require callers to acknowledge returned results.",
instead_of: "Allowing bare calls to selected failure-returning APIs.",
fix_hint: "Add @(require_results) to the selected procedure declaration.",
evidence: "Compiler entities select canonical final-result suffixes; the compiler rejects attributed bare calls but accepts explicit discards.",
cost: "Suffix collisions can select unrelated types; aliases use canonical names. Private APIs and non-final error results are outside this rule.",
severity: "error",
role: "domain",
check: { kind: "require_attribute", attribute: "require_results", on: "exported_procs", roles: ["domain"] },
---

```odin prelude
#+vet explicit-allocators
Domain_Failure :: enum { None, Invalid }
```

```odin fires
#+vet explicit-allocators
validate :: proc() -> Domain_Failure { return .None }
```

```odin silent
#+vet explicit-allocators
@(require_results)
validate :: proc() -> Domain_Failure { return .None }
```
