#+vet explicit-allocators
package {{name_snake}}

// {{Name}}: what this package owns, in one sentence.

{{Name}}_Error :: enum {
	None,
	Invalid,
}

{{Name}} :: struct {
	value: int,
}

// make_{{name_snake}} builds a {{Name}}; the caller owns nothing, so there is nothing to free.
@(require_results)
make_{{name_snake}} :: proc(value: int) -> (out: {{Name}}, err: {{Name}}_Error) {
	if value < 0 {
		return {}, .Invalid
	}
	return {{Name}}{value = value}, .None
}
