{
  version: 1,
  roles: { edge: ["a", "b", "mismatch"] },
  layering: { edge: { may_import: ["edge", "core:*"] } },
  disabled: { "allocators/R1": "fixture: only compiler diagnostics are under test here" },
  odin: { flags: ["-vet", "-warnings-as-errors"] },
}
